node_modules
dist
coverage
pnpm-lock.yaml
*.tsbuildinfo

# Dual-review outputs (gitignored): transient review prose, not repo source.
review-*.md

# The shipwright workflow tree (gitignored via .git/info/exclude): generated
# initiative/run artifacts, not repo source — same class as review-*.md above.
.shipwright

# Generated Prisma client
apps/portal/src/db/generated

# Hand-maintained planning docs — prose, not subject to code style.
docs

# Example apps are standalone projects with their own build, not platform source.
examples

# A template, not prose: Prettier reflows its `<!-- IF:… -->` conditional markers
# into headings and adds trailing commas to the manifest sample, which would ship
# uncopyable JSON to an agent.
packages/deploy-skill/SKILL.md

# Docs site content — prose (like docs/), including the generated quickstart page.
apps/docs/src
